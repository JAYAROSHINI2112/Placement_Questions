// You are using GCC
#include<stdio.h>
int main()
{
    int n,m,a[100],b[100],ptr = 0, i;
    scanf("%d %d",&n,&m);
    for(i = 0; i < n; i++)
      scanf("%d",&a[i]);
    for(i = 0; i < m; i++)
      scanf("%d",&b[i]);
    
    if(n != m)
      printf("Two teams Are Not equal");
    else
    {
        for(i = 0; i < n; i++ )
        {
            if(a[i] != b[i])
             ptr++;
        }
    
    if(ptr > 0)
     printf("Two teams Are Not equal");
    else
      printf("Two teams Are Equal");
    }
      
}
